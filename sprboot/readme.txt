使用spring-boot-maven-plugin可以生成可运行的单体jar，然后使用以下命令启动：
java -Dserver.port=9999 -jar sprboot-0.0.1-SNAPSHOT.jar &

1、演示如何使用spring boot快速开发api项目；
2、演示如何通过maven的spring-boot-maven-plugin插件打包springboot的可运行单体jar；

